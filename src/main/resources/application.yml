spring:
  application:
    name: full-curse-spring-boot
  datasource:
    url: jdbc:mysql://localhost:3306/spring_curse?useTimezone=true&serverTimezone=UTC
    username: root
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
    open-in-view: false
  logging:
    level:
      br.com.dev.osorio.full_curse_spring_boot: DEBUG
  flyway:
    enabled: true
    locations: classpath:db/migration